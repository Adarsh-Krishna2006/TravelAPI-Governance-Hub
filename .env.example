# TravelAPI Governance Hub Environment Variables

PORT=5000
NODE_ENV=development

# Required in production! The application will fail safely at startup if missing in production.
# Minimum 32-character random secret key recommended for production.
JWT_SECRET=travelsphere-super-secret-production-key-change-me

# Optional: Restrict allowed frontend origins in production
FRONTEND_URL=http://localhost:5173
